import App from "./components/MainApp/App";
import { render } from "react-dom";
import * as React from "react";

const container = document.getElementById("rumin_body");
render(<App />, container);