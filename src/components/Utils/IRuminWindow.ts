/**
 * Extend the window object with known properties we put into the global namespace.
 *
 * @interface IRuminWindow
 */

export interface IRuminWindow {
    /**
     * django object
     */
    django: any;
}